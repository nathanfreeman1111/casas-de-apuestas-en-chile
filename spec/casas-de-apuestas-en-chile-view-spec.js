'use babel';

import CasasDeApuestasEnChileView from '../lib/casas-de-apuestas-en-chile-view';

describe('CasasDeApuestasEnChileView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
